{
  "name": "neon-academy-2060",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "check:themes": "node scripts/check-theme-contrast.mjs",
    "build:data": "node scripts/build-question-bank.mjs",
    "build": "npm run check:themes && npm run build:data && vite build",
    "start": "node server.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "8.1.5"
  }
}
