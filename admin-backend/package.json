{
  "name": "neon-riddle-admin-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Secure Firebase Authentication administration API for NEON Academy 2060.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch --env-file=.env src/server.js",
    "check": "node --check src/server.js && node --check src/config/firebase.js && node --check src/middleware/auth.js && node --check src/routes/admin-users.js && node --check src/services/audit.js"
  },
  "dependencies": {
    "cors": "2.8.6",
    "express": "5.2.1",
    "express-rate-limit": "8.6.0",
    "firebase-admin": "14.2.0",
    "helmet": "8.3.0"
  }
}
